// Package forwardprotocol provides definitions and a reference implementation for Fluentd "Forward" protocol
//
// Functions here are not considered performance critical
package forwardprotocol

// This is required to touch fields that are only used in reflection to prevent lint error
func unusedStruct(value struct{}) {
	// This function is required to touch fields that are only used in reflection to prevent lint error
}
